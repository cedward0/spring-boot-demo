runtime: java8
instance_class: F1
runtime_config:
  jdk: openjdk8
env_variables:
  SPRING_PROFILES_ACTIVE: "default"
handlers:
  - url: /.*
    script: this field is required, but ignored
manual_scaling:
  instances: 1
